name: Render Keep Alive - Hourly
on:
  schedule:
    - cron: "0 * * * *"  # 每小时一次
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s --max-time 30 "memos-stable-e4bk.onrender.com" > /dev/null
